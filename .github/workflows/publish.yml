# Heavily influenced by: https://docs.github.com/en/actions/use-cases-and-examples/publishing-packages/publishing-docker-images#publishing-images-to-github-packages
name: Create and publish a container image

# Trigger the build on every tagged release
on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"

# Define registry information for later use
env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

# Build the container using podman
jobs:
  build-and-push-image:
    runs-on: ubuntu-latest

    # Needed permissions for reading the code and publishing a container image
    permissions:
      contents: read
      packages: write
      attestations: write
      id-token: write
    steps:
      # Get the code
      - name: Checkout repository
        uses: actions/checkout@v4

      # Login to the registry early so that we can fail early otherwise
      - name: Log in to the Container registry
        uses: docker/login-action@65b78e6e13532edd9afa3aa52ac7964289d1a9c1
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Inject container metadata into the env for other steps
      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@9ec57ed1fcdbf14dcef7dfbe97b2010124a938b7
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            # set latest tag for default branch
            type=raw,value=latest,enable={{is_default_branch}}

      # Actually build the image and push to ghcr.io
      - name: Build and push Docker image
        id: push
        uses: docker/build-push-action@f2a1d5e99d037542a71f64918e516c093c6f3fc4
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

      # This step generates an artifact attestation for the image, which is an unforgeable statement about where
      # and how it was built. It increases supply chain security for people who consume the image.
      # For more information, see "[AUTOTITLE](/actions/security-guides/using-artifact-attestations-to-establish-provenance-for-builds)."
      - name: Generate artifact attestation
        uses: actions/attest-build-provenance@v1
        with:
          subject-name: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          subject-digest: ${{ steps.push.outputs.digest }}
          push-to-registry: true
